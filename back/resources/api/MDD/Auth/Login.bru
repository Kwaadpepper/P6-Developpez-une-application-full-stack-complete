meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:3001/api/auth/login
  body: json
  auth: none
}

body:json {
  {
        "login": "John Doe",
        "password": "password"
  }
  
}

tests {
  let response = res.getBody();
    bru.setVar("jwt_token", response.token);
  
}
